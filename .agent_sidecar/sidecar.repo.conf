# =============================================================================
# sidecar.repo.conf - Agent Sidecar Configuration (obsidian-cortex)
# =============================================================================
# Team overrides for this repository. Committed to version control.
# For personal overrides, use sidecar.local.conf (gitignored).
# =============================================================================

# No extra apt packages needed - Obsidian is installed via build-extra.repo.sh
# (extracted AppImage, no libfuse2 required)
